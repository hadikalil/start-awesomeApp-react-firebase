import React from "react"

//we'er gonna get vack an object that gives us a provider and consumer  ---> call it FirebaseContext
const FirebaseContext = React.createContext(null)

export default FirebaseContext